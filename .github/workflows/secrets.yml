name: Get AWS SSM Secret

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    env:
      build_stage: "dev"
    steps:
      - name: Configure AWS credentials
        id: aws-credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
        
      # Get all secrets that begin with a prefix
      # IAM user with policy allows the secretsmanager:ListSecrets action
      - name: Get Secret Names by Prefix
        uses: aws-actions/aws-secretsmanager-get-secrets@v1
        with:
          secret-ids: |
            beta*

      # # Parse JSON in secret
      # IAM user with policy allows the secretsmanager:GetSecretValue action
      # - name: Get Secrets by Name
      #   uses: aws-actions/aws-secretsmanager-get-secrets@v1
      #   with:
      #     secret-ids: |
      #       /prod/secret
      #     parse-json-secrets: true

      - name: "Build & Deploy"
        run: |
          echo "You will now have access to the database_url => ${{env.database_url}} and database_password => ${{env.database_password}} environment variables in all your subsequent build steps"